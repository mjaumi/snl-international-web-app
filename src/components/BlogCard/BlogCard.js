import React from 'react';

const BlogCard = () => {
    // rendering blog card component here
    return (
        <div>
            <p>blog card</p>
        </div>
    );
};

export default BlogCard;